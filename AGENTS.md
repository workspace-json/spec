# Workspace Notes

- Keep package entry points aligned with `./packages/spec/src/index.ts`, `./packages/rules/src/index.ts`, and `./packages/agents-audit/src/index.ts`.
- Review changes against `./packages/agents-audit/src/cli.ts` before release.
- Keep workspace metadata in `./CHANGELOG.md` and `./README.md` current.
- Workspace layout is defined in `./pnpm-workspace.yaml`.
